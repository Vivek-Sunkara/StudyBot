Machine Learning Basics

Overfitting occurs when a model learns the training data too closely and performs poorly on unseen data.
Regularization reduces model complexity and can help control overfitting.
Gradient descent updates model parameters in a direction that reduces the objective function.

Database Basics

A primary key uniquely identifies a row in a relational table.
A foreign key references a key in another table and represents a relationship between tables.
